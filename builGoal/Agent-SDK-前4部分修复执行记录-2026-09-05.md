# Agent-SDK 审计方案前 4 部分修复执行记录（2026-09-05）

> 对应报告：《Agent-SDK-全项目问题审计与技术重构方案-2026-09-05.md》§4/§5/§6/§7。
> 本记录只写已落盘的代码修改与已真实运行的验证；未完成项明确标注，不把计划当交付。
> 工作树说明：仓库存在大量更早轮次的未提交变更，本记录的"文件清单"指本轮修复触及的文件与语义，不代表全部工作树状态。

## 0. 批次划分与执行方式

| 批次 | 内容 | 执行方式 |
|---|---|---|
| 本轮（第一批） | §4 全链路握手契约（服务端+壳）、§6.1 前端请求收敛、§5 可落地 UI 修复、§7 第一批（ToolEffect 元数据） | 5 路并行子代理，文件分区互斥 |
| 下一批（计划） | §6.2 只读工具并发调度（JoinSet）、§6.3 DeadlineBudget、§7 第二批（权限模式/GrantStore/审批卡 UI/CLI --permissions）、§5 ES modules 拆分与约束控件化 | 待第一批集成验证后接续 |

## 1. §4 桌面端空壳修复（P0）

### 1.1 跨模块契约（新增，双侧逐字对齐）

| 契约项 | 定义 |
|---|---|
| 实例身份环境变量 | `OWO_DESKTOP_INSTANCE_ID`（uuid simple；未设置 = 开发模式） |
| 实例身份请求头 | `x-owo-desktop-instance`（恒定时间比较） |
| ready 握手行 | stdout 单行 JSON：`{"event":"core_ready","pid","port","api_version","build_id","instance_id"}` |
| 动态端口 | `serve --port 0` → 系统分配，实际端口以 ready 行 / `listener.local_addr()` 为准 |
| /health 新增 | `instance_id`（未设为 null）、`pid`、`stage:"ready"`、`build_id`；既有字段保留 |
| /auth/token 门控 | pairing 门控之后追加实例门控；不匹配 → 403 `auth/instance_mismatch/not_retryable` |
| /server/shutdown 门控 | confirm 校验前先实例门控；不匹配 → 403（服务不被外来方关闭） |
| 错误码族 | `core/binary_missing` / `core/handshake_timeout` / `core/identity_mismatch` / `core/spawn_failed` / `core/exited` + §4.3 中文用户文案 |
| 壳 IPC（新增） | `get_core_state` / `get_core_connection` / `retry_core_start` / `open_core_logs` |

### 1.2 服务端 + CLI serve 侧（代理 A）

待回填：改动文件、要点、测试证据。

### 1.3 Tauri 壳侧（代理 B）

待回填：core_runtime 状态机、单实例锁、日志捕获、重启退避、updater 占位处理、测试证据。

### 1.4 核心侧自动化验收（§4.4 可自动化子集）

- 脚本：`agent-sdk/scripts/verify-desktop-handshake.ps1`（新增）
- 覆盖：`--port 0` 动态端口、ready 行解析、/health 身份字段、/auth/token 与 /server/shutdown 实例门控 403/200 矩阵、错误实例头不关停、正确实例头优雅退出、开发模式兼容对照。
- 待回填：运行输出。

### 1.5 需真实桌面会话的剩余项（明确不在本轮）

窗口可见性、双击双开、sidecar 缺失页、GUI 下的日志入口点击——留待 §8 桌面 E2E 批次。

## 2. §5 UI 信息架构与表单（第一批）

待回填：7 项问题→文件→修法对照表、rail 裁切根因、测试证据。

## 3. §6 对话与工具调用性能

### 3.1 §6.1 前端请求收敛（代理 C）

待回填：单飞恢复控制器、首屏 hydrate 前后对比、ROUTE_LOADERS 表、定时器治理、Tauri 连接消费、测试证据。

### 3.2 §6.2 工具并发调度（下一批）

已侦察：`agent.rs` L391 `for call in calls` 顺序审批+执行为放大点；等 §7 第一批（PermissionRequest 注入点）落地后实施 JoinSet 并发 + 按原始顺序重组。

## 4. §7 权限与审批体系（第一批）

待回填：ToolEffect/EffectClass 注册表、内置工具映射表、MCP annotations 规范化、Policy::level_for 判定变化表、风险注记、测试证据。

## 5. 集成验证总表

待回填：cargo fmt/clippy/test（core/cli/server/desktop）、node --test 全量、verify-desktop-handshake.ps1 输出。

## 6. 遗留与下一批

待回填。
