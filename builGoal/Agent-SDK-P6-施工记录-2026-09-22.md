# Agent SDK P6 增量施工记录：当前源码桌面壳冷启动验收

依据：`builGoal/Agent-SDK-后续任务实施指南-2026-09-18.md` §8 P6、§9.1、§10。

## 本轮范围

- 源码工作树：dirty；当前 SDK HEAD：`fc8251d42aa192715fa9f3208585802fa87d88ef`。
- 所有构建使用 T 盘默认 target；执行前走 `Resolve-OwoOrtEnv` 与 `Invoke-CiCargo`，桌面壳构建严格串行 `-j 1`。
- 未生成 release 安装包；本记录只确认 debug 桌面壳与当前 debug sidecar 的隔离冷启动和重连行为。

## 产物身份

1. 使用当前源码重建 `desktop/tauri/src-tauri/target/debug/owo-agent-desktop.exe`，构建 exit 0。
2. 使用 `scripts/stage-desktop-sidecar.ps1 -Configuration debug -NoBuild -Json` 清理旧 binaries 并重新暂存当前 `target/debug/owo-agent.exe`。
3. staging SHA-256：`CB357EC8ACF8EE7E2F167BDA7EECF05E0C7C48452CFD188E06F7416B3C15E6A3`。
4. 壳内 sidecar `--version` 与当前 HEAD 一致：
   `commit=fc8251d42aa192715fa9f3208585802fa87d88ef dirty=true`。

此前壳 Clippy/测试曾报告 binaries 中 sidecar 是旧 commit `585d178...`；本轮 staging 已修复该错配，随后桌面壳严格 Clippy exit 0。

## 真机隔离冷启动

命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-desktop-cold-boot.ps1 `
  -SkipHiddenWindow -SkipScreenshots `
  -ShellExe desktop\tauri\src-tauri\target\debug\owo-agent-desktop.exe `
  -SidecarExe target\debug\owo-agent.exe `
  -EvidenceDir docs\qa\evidence\p6-coldboot-20260922-0603
```

结果：**31/31 通过，exit 0**。证据目录：
`agent-sdk/docs/qa/evidence/p6-coldboot-20260922-0603/`。

随后使用同一重建壳和 sidecar 执行 `-HiddenMinutes 1 -SkipScreenshots`，结果
**38/38 通过，exit 0**。证据目录：
`agent-sdk/docs/qa/evidence/p6-coldboot-hidden1m-20260922-0604/`。隐藏期服务端 ledger
增量为 0，health/business/轮询请求均为 0；唤回后 UI 退出后台态并补刷隐藏期间积累的域。

覆盖当前源码真实运行态：

- 可见桌面窗口、HWND 归属、尺寸与 WebView2 CDP；
- sidecar `core_ready`、`/health`、API 版本、实例身份和 build id 一致；
- 首屏可输入、无错误卡、无误落 Provider 引导页；
- 首屏业务请求 ≤5、无重复路由、WebView 无 `/auth/token`、事件流唯一；
- 强杀 core 后壳自动重启，窗口实例身份稳定、core PID 变化、token 换代；
- 上一代 token 返回 401，新 token 返回 200；重启后界面恢复可输入且不形成双事件连接。

## 已知非阻断提示

启动日志出现 `HotKey already registered`，壳继续运行且上述全部断言通过；它属于全局快捷键注册冲突提示，不等同于核心/窗口启动失败，后续可作为桌面体验项单独处理。

## 尚未因此完成的 P6/P2 门

- 本轮未跑隐藏窗口 5 分钟、release 安装包、安装后双击黄金路径、三端真实模型固定任务 20 次/项。
- P2 固定性能任务报告仍未覆盖；P3 ToolHostService/统一 receipt sink 仍未完成。
- 全 workspace 测试序列此前在 worker subprocess 用例处停滞并被中断；`goal_api_tests` 单独严格串行 32/32 通过，不能据此宣称全 workspace 通过。

## Release 产物增量验收（2026-09-22）

本轮补齐的是本机 dirty 工作树下的 release 验收，不是可发布 clean commit。构建前后均使用 T 盘 `agent-sdk` target；核心 release 构建走统一 ORT 解析和 strict `-j 1` 门禁。因工作树存在未提交改动，使用 `OWO_ALLOW_DIRTY_RELEASE=1`，所有证据均明确记录 `dirty=true`，不得把本轮安装包当作正式发布物。

### 产物与身份

- release 核心构建成功：`target/release/owo-agent.exe`。
- release sidecar 重新暂存到 `desktop/tauri/src-tauri/binaries/owo-agent-x86_64-pc-windows-msvc.exe`，源与暂存 SHA-256 均为 `EECBCB3B0DB40EBF316979CC86DDC0D6A4E795A3D06A2E3D40DBC22CB6099ACB`。
- 桌面壳重新链接并生成 NSIS：`desktop/tauri/src-tauri/target/release/owo-agent-desktop.exe`、`desktop/tauri/src-tauri/target/release/bundle/nsis/OwO Agent_0.1.0_x64-setup.exe`。
- `release-manifest.json` 记录核心、桌面壳、安装包、sidecar 四项哈希，commit=`fc8251d42aa192715fa9f3208585802fa87d88ef`，api=`0.7`，dirty=`true`，核心 `built_at=2026-09-21T23:32:43Z`。
- 证据目录：`agent-sdk/docs/qa/evidence/p6-release-20260922-0850/`。

### 发现并修复的发布链问题

1. `build-installer.ps1` 原先把 npx 默认写死到不存在的 `D:\前端框架\npx.cmd`；现改为显式 `OWO_NPX` 优先，否则从 PATH 解析 `npx.cmd`。
2. Tauri 壳链接会继承 `agent-sdk/.cargo/config.toml` 的 `/NODEFAULTLIB:LIBCMT`，与 `static_vcruntime` 的参数冲突；发布脚本现在仅在 Tauri 子进程中清空两种 Cargo Rustflags，核心构建门禁不变。
3. 首次打包后发现 binaries 中仍是旧 debug sidecar；已重新执行 release `stage-desktop-sidecar -NoBuild`，重打壳和安装包，并以 release `built_at=23:32:43` 的真实 sidecar 重验。不能仅凭 commit 相同接受跨配置产物。
4. `verify-desktop-cold-boot.ps1` 原先固定按 debug 暂存，即使传入 release sidecar 也会测到旧 debug 文件；现根据 `-SidecarExe` 推导暂存配置并写入证据，避免假绿。

### Release 真机/配对结果

- 使用 release 桌面壳 + release sidecar + 私有用户数据目录执行 `verify-desktop-cold-boot.ps1 -HiddenMinutes 1 -SkipScreenshots`：**38/38 通过，exit 0**。
- 覆盖真实窗口/WebView2、core ready、API/build identity、首屏业务请求 ≤5、事件流唯一、强杀 core 后自动重启、token 换代、旧 token 401、新 token 200、隐藏 1 分钟零轮询/零业务请求、唤回补刷；唯一提示仍为非阻断的 `HotKey already registered`。
- 证据目录：`agent-sdk/docs/qa/evidence/p6-release-20260922-0850/coldboot-release-v2/`。
- `verify-release-pairing.ps1 -CliPath target/release/owo-agent.exe`：exit 0；无配对头 403、正确配对头 200（token 长度 64）、错误配对头 403。
- 随后执行真实桌面故障矩阵 `verify-desktop-failure-matrix.ps1 -SkipScreenshots`：**8 个场景、74/74 通过，exit 0**；覆盖 `binary-missing`、`identity-mismatch`、`no-workspace`、`provider-unset`、`data-dir-unwritable`、`example-mcp-off`、`core-exit`、`core-hang`，并通过 debug core 哈希前后一致守卫。证据目录：`agent-sdk/docs/qa/evidence/p6-release-20260922-0850/failure-matrix/`。
- 故障矩阵结束后已重新暂存 release sidecar，当前 `binaries/` 不留 debug 暂存；最终 release sidecar SHA-256 仍为 `EECBCB3B0DB40EBF316979CC86DDC0D6A4E795A3D06A2E3D40DBC22CB6099ACB`。

### 仍未完成的发布门

- dirty 树尚未形成可发布 clean commit；尚未完成隔离安装器双击后的完整 CLI/Desktop 黄金路径。
- 固定性能任务尚未形成真实 8 项×20 样本报告；全 workspace 串行验收、24 小时 soak、完整故障矩阵仍未全部通过。
- 黄金路径中的真实模型请求、文件读取/写入、审批、diff/revert、CLI/Desktop 跨端会话同步仍需继续逐项取证；本轮不能据此写“P6 完成”或“现有架构功能闭环完成”。

## 最新源码对应的 Release 重建（2026-09-22）

在 P3 capability 生命周期增量后，重新执行已修复的 `scripts/build-installer.ps1 -Configuration release`，
确保发布产物不早于当前源码。核心 strict release 构建 exit 0（16m34s），Tauri 壳和 NSIS
打包 exit 0；脚本自动使用 PATH 中的 `npx.cmd`，构建期间临时隔离 workspace CRT 配置并在
`finally` 中恢复，最终确认 `.cargo/config.toml` 存在且无遗留 backup。

最新清单与哈希位于：`agent-sdk/docs/qa/evidence/p6-release-20260922-0920/`。

- `owo-agent.exe` / release sidecar：61,810,176 bytes，SHA-256
  `B44FF5A74B1B2A3428E1F0724CC67196432C7A4F7A9103B1D8CD601AED50FB3F`。
- `owo-agent-desktop.exe`：12,760,576 bytes，SHA-256
  `43191DF273F2B81556AA63B1403E08F03F7431A755F0817330C1E97225F3D991`。
- `OwO-Agent-0.1.0-x64-setup.exe`：16,879,883 bytes，SHA-256
  `89C1CA99D4E3406A9332D766E1E5789D7F562CA104D392CBDAD3E0D8DC95A1CB`。
- manifest 记录四项产物、commit=`fc8251d42aa192715fa9f3208585802fa87d88ef`、api=`0.7`、
  dirty=`true`、toolchain/rust target/ORT 版本；dirty override 仍只代表本机验收，不代表正式发布。
- 使用这次最新 release 壳和 sidecar 的隔离冷启动：**38/38 通过，exit 0**；证据目录
  `agent-sdk/docs/qa/evidence/p6-release-20260922-0920/coldboot-release/`。

本轮仍只证明当前 dirty 树的 release artifact acceptance；clean commit 发布、隔离安装器双击
黄金路径、真实模型固定性能报告和完整 CLI/Desktop 工具闭环仍未完成。

## 最新源码对应的 Release 重建与正确 release sidecar 验收（2026-09-22 10:00）

由于上一轮新增了 `ToolApprovalGrant` 类型边界，重新执行 `build-installer.ps1 -Configuration
release`，核心 strict `-j 1` 构建 exit 0（16m41s），Tauri release 壳 exit 0（3m27s），
NSIS bundle exit 0。构建使用 `OWO_ALLOW_DIRTY_RELEASE=1`，仅作为本机 dirty 树验收，未提交
或清理工作树。

### 当前产物身份

- `target/release/owo-agent.exe`：61,999,616 bytes，SHA-256
  `CD4713CE5ADDDD8375815471004BB3D3E67380437532DBF2F211D05041048795`。
- `desktop/tauri/src-tauri/binaries/owo-agent-x86_64-pc-windows-msvc.exe`：同上；
  release staging 与核心源文件哈希一致。
- `desktop/tauri/src-tauri/target/release/owo-agent-desktop.exe`：12,760,576 bytes，SHA-256
  `355B9061F804A33610115C4F9C83A1A791197EA8E42597E922F00D6F82BFCB78`。
- `desktop/tauri/src-tauri/target/release/bundle/nsis/OwO Agent_0.1.0_x64-setup.exe`：
  16,906,786 bytes，SHA-256
  `B818A0A012DFDA52AAEDCF507CAED590AC786B58F3CC43A89CD16D310984D481`。
- manifest：`agent-sdk/docs/qa/evidence/p6-release-20260922-1100/release-manifest.json`；
  commit=`fc8251d42aa192715fa9f3208585802fa87d88ef`、api=`0.7`、dirty=`true`，核心
  `built_at=2026-09-21T23:32:43Z`。

### 验收证据

- `verify-release-pairing.ps1 -CliPath target/release/owo-agent.exe` exit 0：无配对头 403、
  正确配对头 200（token 长度 64）、错误配对头 403。
- 使用 release 壳和 `target/release/owo-agent.exe` 作为 sidecar 源执行
  `verify-desktop-cold-boot.ps1 -HiddenMinutes 1 -SkipScreenshots`：**38/38 通过，exit 0**。
  证据目录：`agent-sdk/docs/qa/evidence/p6-release-20260922-1100/coldboot-release/`；
  `binaries.json` 明确记录 `stage_configuration=release`，启动私有 bin 与 staged SHA-256
  均为 `CD4713...48795`。覆盖窗口/WebView2、core ready、首屏 ≤5、事件唯一、core 重启、
  token 换代、旧 token 401、新 token 200、隐藏 1 分钟零 health/业务/轮询和唤回恢复。

此前 `p6-release-20260922-1035` 的一次冷启动命令误把旧 `src-tauri/binaries` 文件作为
`-SidecarExe`，脚本按 debug 配置暂存；该目录不作为本轮 release 证据，已用正确
`target/release/owo-agent.exe` 重跑并单独记录。这个误用暴露了验收参数风险，但没有修改
源码构建结果。

边界不变：dirty 树、安装器隔离双击后的完整黄金路径、8 项×20 真实性能报告、全 workspace
串行/24 小时 soak，以及普通文件写入的 snapshot→diff→receipt/ChangeSet 原子闭环仍未完成，
因此不能据此宣称指南任务全部完成。

## 最新源码对应的 Release 重建与冷启动复验（2026-09-22 10:49）

P3 新增普通文件执行回执后，重新执行 `scripts/build-installer.ps1 -Configuration release`。
核心 strict `-j 1` 构建 exit 0（14m18s），Tauri release 壳构建约 3m29s，NSIS bundle
exit 0；构建前 T 盘剩余约 23.27 GB，未触发清理门，所有 target/cache 仍在 T 盘。
构建使用 `OWO_ALLOW_DIRTY_RELEASE=1`，因此这些是当前 dirty 工作树的验收产物，不是正式发布物。

当前产物哈希：

- `target/release/owo-agent.exe`：62,261,760 bytes，SHA-256
  `32AAFF920919073A45766F29D5DD0B9B5E11930E7B4E023D52DC689BAD164484`。
- `desktop/tauri/src-tauri/target/release/owo-agent-desktop.exe`：12,760,576 bytes，SHA-256
  `BA1618CD85A8C647A46284424B8E4885A912948158D8ED0279CC37A52CED8F0F`。
- `desktop/tauri/src-tauri/target/release/bundle/nsis/OwO Agent_0.1.0_x64-setup.exe`：
  16,967,343 bytes，SHA-256
  `5649097200E39F310656C1E27349162B7CC98548E1AA7046F0079BFBD699A7AE`。
- release sidecar staging 与 `target/release/owo-agent.exe` 在构建脚本中已完成复制哈希核对；
  当前 core 身份为 commit=`fc8251d42aa192715fa9f3208585802fa87d88ef`、api=`0.7`、
  dirty=`true`、`built_at=2026-09-21T23:32:43Z`。核心 manifest 见
  `agent-sdk/docs/qa/evidence/p6-release-20260922-104905/release-manifest.json`。

使用 release 桌面壳和 `target/release/owo-agent.exe` 作为 sidecar 源，私有数据目录执行
`verify-desktop-cold-boot.ps1 -HiddenMinutes 1 -SkipScreenshots`：**38/38 通过，exit 0**。
证据目录：`agent-sdk/docs/qa/evidence/p6-release-20260922-104905/`。覆盖首屏可输入、
健康/实例/build identity、WebView2、首屏业务请求预算、唯一事件流、core 强杀后自动重启、
旧 token 401/新 token 200、隐藏 1 分钟零轮询/零业务请求、唤回恢复；唯一提示仍为非阻断的
`HotKey already registered`。

边界：本轮只更新了 release 产物和真实冷启动证据；dirty 树正式发布、隔离安装器双击后的
完整黄金路径、真实模型固定性能报告、全 workspace/24 小时 soak，以及 P3 普通文件写入的
UI/CLI/Desktop/MCP 全链路仍未完成，不能据此宣称 P6 或指南任务全部完成。

## 最新源码对应的增量 Release 复验（2026-09-22 11:11）

为纳入 `repl_daemon.rs` 的 Clippy 结构修复，重新执行增量 release 构建：核心 exit 0
（2m43s），Tauri 壳约 3m40s，NSIS exit 0。当前 T 盘剩余约 22.15 GB，仍未触发清理；
构建和缓存继续位于 T 盘。dirty override 仍为 1，产物仅作本机验收。

- `target/release/owo-agent.exe`：62,261,760 bytes，SHA-256
  `47858164D1A4D9A2D7BB1E79D977376AA824F96FCD88DDC7F1DA1A013A7A8607`。
- `desktop/tauri/src-tauri/target/release/owo-agent-desktop.exe`：12,760,576 bytes，SHA-256
  `FF63CCD70E8516672D65C75610B59157D0AE32071FFB1C4110EAE29CE8DC2B58`。
- `desktop/tauri/src-tauri/target/release/bundle/nsis/OwO Agent_0.1.0_x64-setup.exe`：
  16,960,677 bytes，SHA-256
  `7F9082C9FF6FAB1ED3494FE9603A67D9228B58873A8E5721527EECAE16D61A5D`。
- manifest 与冷启动证据：
  `agent-sdk/docs/qa/evidence/p6-release-20260922-111121/`；manifest 记录核心哈希
  `478581...A8607`、commit=`fc8251d42aa192715fa9f3208585802fa87d88ef`、api=`0.7`、
  dirty=`true`。release sidecar staging 与核心源文件由构建脚本完成复制哈希核对。
- 使用 release 壳 + release sidecar 的隔离冷启动 **38/38 通过，exit 0**；同轮
  `verify-release-pairing.ps1` 通过：无配对头 403、正确配对头 200、错误配对头 403。

本次仍不改变完成边界：dirty 树正式发布、隔离安装器双击后的完整黄金路径、真实模型固定
性能样本、全 workspace/24 小时 soak，以及 P3 的完整 UI/CLI/Desktop/MCP 写入审批→diff→
receipt→revert 全链路仍未完成。

## 最新 release 客户端工具闭环证据（2026-09-22 11:55）

基于本节 11:11 产物，用本地确定性 OpenAI-compatible 夹具复验真实客户端文件闭环：CLI
`turn`/Daemon REPL 与 release Desktop 均完成 `write_file → diff → revert`，证据目录为
`agent-sdk/docs/qa/evidence/client-tool-golden-20260922-115500/`。Desktop 日志确认 turn、
diff、revert 路由均 200，回滚后目标文件不存在；CLI stdout 记录工具结果、最终文本和 diff。

该轮 Desktop 未出现审批条，工具被当前隔离运行自动放行；证据报告明确保留这个边界，未把
自动放行写成审批点击通过。因此这补齐了当前 release 的真实文件闭环证据，但不替代安装器
双击验收、强制审批 UI、真实模型性能样本、clean 发布树和全 workspace/24 小时 soak，P6/指南
任务仍不能宣称全部完成。

## 当前源码对应的审批策略修正与 release 复验（2026-09-22 12:25）

按指南 §4.5 修正 `PermissionProfile::Workspace`：工作区内写入首次进入审批，只有审批生成
的 Grant 命中后才自动放行。策略 59/59、core 187/1 ignored、server 路由 32/32 通过后，
重新构建 release 核心、桌面壳和 NSIS，均 exit 0。

- core：62,261,760 bytes，SHA-256
  `14E10DE780EE00C298ECD493F508248966CD19C78FC3732E9341775FA5929128`。
- Desktop shell：12,760,576 bytes，SHA-256
  `BE619FAC9F6C4F594EF2FB4480BF2A393D04B065D846B9DB0E2B7937E17A9977`。
- NSIS：16,971,389 bytes，SHA-256
  `F0F661448F1E5BF05DB6FE580BD5F73E53A8D44BEF438A9E6B2EBBFABE3015DE`。

当前 release Desktop 私有环境真实复验已取得：审批条出现、允许一次、工具写入、diff 展示、
回滚删除目标文件；`turn`、permission、diff、revert 路由均有 200 日志。证据目录：
`agent-sdk/docs/qa/evidence/desktop-tool-approval-20260922-122500/`。报告明确注明验收脚本
只在通用清理阶段被中断，业务断言已由 WebView、服务日志和隔离文件事实完成。

剩余发布边界不变：安装器双击后的独立完整黄金路径、真实模型固定性能任务、故障矩阵全量、
clean 发布树与全 workspace/24 小时 soak 尚未完成，不能据本节宣称指南任务全部完成。

## 当前 release 冷启动最终复验（2026-09-22）

使用本节 12:25 生成的 release 核心、桌面壳和 sidecar，执行隔离私有数据目录的
`verify-desktop-cold-boot.ps1 -HiddenMinutes 1 -SkipScreenshots`，**38/38 通过，exit 0**。
证据目录：`agent-sdk/docs/qa/evidence/p6-release-20260922-122500/coldboot/`。

本轮确认了当前产物的 core/壳身份一致、健康与配对握手、首屏可输入、唯一事件流、首屏请求预算、
core 强杀后自动重启、旧 token 401/新 token 200、窗口隐藏 1 分钟期间零轮询零业务请求，以及唤回后的
界面恢复；最终无残留 cargo、rustc、link、owo-agent 或桌面壳进程。T 盘剩余约 22.30 GB，未触发清理。

这只完成了当前 release 的冷启动验收，不改变完成边界：安装器双击后的独立完整黄金路径、真实模型
固定性能任务、故障矩阵全量、clean 发布树、全 workspace/24 小时 soak，以及 P3 普通文件写入的
UI/CLI/Desktop/MCP 完整审批→diff→receipt→revert 全链路仍未完成。因此指南任务整体仍不能宣称完成。

## R3 桌面真实故障矩阵复验（2026-09-22）

使用当前 debug 桌面壳执行 `verify-desktop-failure-matrix.ps1` 全 8 场景，**82/82 通过，exit 0**；
证据目录：`agent-sdk/docs/qa/evidence/r3-failure-matrix-20260922/`。矩阵覆盖 binary-missing、
identity-mismatch、no-workspace、provider-unset、data-dir-unwritable、example-mcp-off、core-exit、
core-hang，逐项确认可操作窗口、稳定错误码/引导码、契约动作、技术详情折叠、场景目录隔离与执行阶段。

本轮同时生成各场景 PNG 和结构化 JSON 事实，并验证真实 debug core 哈希前后一致、无改名残留；
唯一反复出现的 `HotKey already registered` 为已有全局快捷键占用提示，不影响矩阵断言，仍属已知非阻断提示。
该结果补齐 R3 故障矩阵证据，但不改变指南整体完成边界：安装器双击独立黄金路径、真实模型固定性能
样本、clean 发布树、全 workspace/24 小时 soak，以及 P3 普通文件写入的 UI/CLI/Desktop/MCP 完整
审批→diff→receipt→revert 全链路仍未完成。

## 当前 NSIS 安装包隔离安装复验（2026-09-22）

使用当前 release NSIS 包在 T 盘证据目录隔离安装，安装器 exit 0，得到桌面壳、core sidecar 和卸载程序。
安装后的 core SHA-256 与 release 源产物一致：
`14E10DE780EE00C298ECD493F508248966CD19C78FC3732E9341775FA5929128`；安装后的桌面壳大小和产品/文件
版本与构建产物一致，NSIS 安装过程仅改变了 3 个 bundle-type 标记字节（`UNK`→`NSS`），已记录在
`agent-sdk/docs/qa/evidence/p6-release-20260922-122500/release-manifest.json`。

使用“安装后的桌面壳 + release core”执行隔离冷启动黄金路径，**38/38 通过，exit 0**；证据目录：
`agent-sdk/docs/qa/evidence/p6-release-20260922-122500/installed-coldboot-release-core/`。覆盖安装包壳的
sidecar 拉起、健康/身份、首屏可输入、重启换 token、隐藏窗口零轮询和唤回恢复。该结果补齐了当前
NSIS 包的隔离安装/启动证据；仍不代表真实模型固定性能、完整故障矩阵之外的 P6 故障项、clean 发布树
和 24 小时 soak 已完成。

## 随包外部搜索工具接入与 NSIS 资源复验（2026-09-22）

按“成熟搜索工具随程序发布”的要求，将 `search_files` 从自写递归遍历切换为随包
ripgrep 14.1.1（官方 Windows x86_64 MSVC 发布包，revision=`4649aa9700`）。固定资源目录为
`agent-sdk/tools/rg/`，包含 `rg.exe`、`manifest.json`、官方 `COPYING`/`LICENSE-MIT`/`UNLICENSE`
和 README；工具 SHA-256 为
`F162B54DE2ADFC72D78ADB1DBADA2DEDDA111AE0A5E2F6E9500F4F909664C5D2`。解析只接受随包目录或
显式测试覆盖目录，不把用户机器上另一个 `rg` 当作产品实现；`run_command` 的已审批命令则将随包
工具目录置于受控 PATH 首位。

权限边界未放宽：`search_files` 仍为只读、工作区 cwd、JobOnly 隔离、30 秒 CPU/进程门和 200 条结果上限；
`run_command` 仍需审批并继续使用既有危险命令 deny 与工作区沙箱。新增 core 单测实际启动随包 rg，
验证大小写不敏感文件名搜索和路径归一；core 测试 **190 passed / 1 ignored**，clippy `-D warnings`
通过。

Tauri 配置新增 resources 映射，build.rs 对工具文件、版本和哈希清单做构建期门禁。当前 release
构建与安装包复验结果：

- core：`target/release/owo-agent.exe`，SHA-256
  `A187CE7A7F7579321E63920AB8297E172D2E22B329807582E6756F071D25A4D1`。
- Desktop shell：`desktop/tauri/src-tauri/target/release/owo-agent-desktop.exe`，SHA-256
  `41688DA5DBC799031F3A2484D2E2D56FAE32556FA0D1FB22F079D318C36B061A`。
- NSIS：`desktop/tauri/src-tauri/target/release/bundle/nsis/OwO Agent_0.1.0_x64-setup.exe`，
  SHA-256 `EA30DD8342ED61DE989F3D17D68FD62D40E269D10E6A53E24F5B89C07AD5C50F`，大小 18,392,015 bytes。
- 隔离安装目录：`agent-sdk/docs/qa/p6-ripgrep-install-20260922/`；安装器 exit 0，安装后
  `tools/rg/rg.exe` 存在、版本为 14.1.1、SHA-256 与源码随包文件一致，许可证和 manifest 均在安装目录。

随后基于本次新实现重新采集固定性能任务 `search_1000_files`：20/20 回合成功，20 条 trace
均带 `performance_task=search_1000_files`，每条事件包含 `search_files` tool start/result，结果
确认命中 `file-0777.txt`；该任务 P50=11,456 ms、P95=20,201 ms。证据目录为
`agent-sdk/docs/qa/evidence/p6-real-perf-20260922-search-1000-rg/`，统一报告已更新到
`agent-sdk/docs/qa/evidence/p6-real-perf-20260922/performance-report.json` 和 `.md`。先前基于旧
递归实现的 retry 证据不再作为本任务结论；统一性能报告仍因其他固定任务缺样本保持 incomplete，
指南整体不能宣称完成。

## 固定性能八项补齐、失败 trace 持久化与当前安装包复验（2026-09-22）

按指南 §6.5 使用真实 release Daemon、真实 HTTP/SSE、真实模型和独立工作区补齐固定性能任务。
写文件任务使用 `--verify-diff` 读取 `/session/{id}/diff`；审批任务每轮实际触发
`run_command` 和 permission request；坏 MCP 任务在启动目录注入无效 stdio 配置；断线任务真实关闭
首个 SSE 客户端、通过 `/session/{id}/turn/events` durable replay 重连，再调用
`/session/{id}/abort`。每项均为 20/20 有效轮，统一证据目录为
`agent-sdk/docs/qa/evidence/p6-real-perf-20260922/`，固定覆盖门已通过：160 条 trace、8 项各 20 条，
`performance-report.json` 状态为 `complete`，无未分类样本。

| 任务 | P50 | P95 | 备注 |
|---|---:|---:|---|
| `daemon_start_session` | 4,452 ms | 4,854 ms | 另有 core_ready P50=223.00 ms、P95=225.58 ms |
| `short_text_conversation` | 7,080 ms | 10,741 ms | 真实模型 |
| `read_100kb_file` | 11,794 ms | 15,899 ms | 真实文件读取 |
| `search_1000_files` | 11,456 ms | 20,201 ms | 随包 ripgrep 14.1.1 |
| `write_file_and_diff` | 11,857 ms | 16,253 ms | 真实审批、写入、diff |
| `approval_command` | 34,216 ms | 131,105 ms | 20/20 有 `run_command` + permission |
| `invalid_mcp_startup` | 4,085 ms | 12,718 ms | 20/20 core_ready，坏 MCP 未阻塞启动 |
| `disconnect_reconnect_cancel` | 76 ms | 90 ms | 取消到停止 P50=292.96 ms、P95=356.94 ms、最大 358.51 ms |

原实现只在成功回合生成 TraceRecord，导致取消回合无法进入性能报告。本轮新增失败/取消 trace
持久化，记录 `error`、真实 duration 和固定任务标签；新 release 二进制重跑断线任务后得到
`disconnect_reconnect_cancel` 20 条 `error` trace，未手工伪造样本。验证结果：core 单元测试
190 passed / 1 ignored，server 全量测试 exit 0，core/server clippy `-D warnings` 通过，fmt 通过。

针对当前源码重新生成 dirty 验收 release（未提交工作树，使用进程级
`OWO_ALLOW_DIRTY_RELEASE=1`）：

- `agent-sdk/target/release/owo-agent.exe`：SHA-256
  `F04FCD619B1B94C82EF7005E360873E7797C3999EDD134A3FC8F306D9BCBF3AB`。
- `agent-sdk/desktop/tauri/src-tauri/target/release/owo-agent-desktop.exe`：SHA-256
  `B6C6756B3B09FC1330A77AC4FC2CFCBDE94D4875BAA480BC9762E2D1316D17B3`。
- `agent-sdk/desktop/tauri/src-tauri/target/release/bundle/nsis/OwO Agent_0.1.0_x64-setup.exe`：SHA-256
  `A10BC72A2AD18EEBFA0AE8111EC5C8DEC7BC836F3F513A3D1C2A39BB2A616A68`。
- 隔离安装目录：`agent-sdk/docs/qa/p6-current-install-20260922/`；安装器 exit 0，安装后 core
  与源 release SHA 一致，`tools/rg/rg.exe` 版本/源 SHA 一致，三份许可证与 manifest 均存在。
- 安装后的 shell + core 执行 `verify-desktop-cold-boot.ps1 -HiddenMinutes 1 -SkipScreenshots`：
  **38/38 通过，exit 0**；证据目录：`agent-sdk/docs/qa/evidence/p6-current-installed-coldboot-20260922/`。

本节完成了 P6 固定性能证据、随包外部搜索工具和当前 NSIS 安装冷启动复验，但不等于指南 §11
全部完成。仍需独立收口的边界包括 clean commit 正式发布树、指南要求的完整 release manifest/sidecar
清单、24 小时 soak 及其内存/SSE/磁盘曲线、所有剩余 P0/P1/P2/P3 验收项和最终任务清单逐项签字。
