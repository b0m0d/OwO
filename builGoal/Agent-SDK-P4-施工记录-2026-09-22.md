# Agent SDK P4 增量施工记录：桌面 WebView 真实 UI 验收

依据：`builGoal/Agent-SDK-后续任务实施指南-2026-09-18.md` §8 P4、§9.1、§10.1。

## 本轮范围

- 验收对象：当前源码重建的 debug 桌面壳与已重新暂存的当前 debug sidecar。
- 当前 HEAD：`fc8251d42aa192715fa9f3208585802fa87d88ef`，工作树 dirty；不把 dirty 构建伪装成 release 产物。
- 验收脚本：`agent-sdk/scripts/verify-desktop-r4-ui.ps1`。
- 本轮使用 `-SkipScreenshots`，因此本记录只计入交互、键盘、业务请求和脱敏证据，不把截图项计入通过项。

## 真实 WebView 验收结果

命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-desktop-r4-ui.ps1 `
  -SkipScreenshots `
  -EvidenceDir 'T:\创新创业\OwO-master\agent-sdk\docs\qa\evidence\p4-r4-ui-20260922-0612'
```

结果：**45/45 通过，exit 0**。

证据目录：
`agent-sdk/docs/qa/evidence/p4-r4-ui-20260922-0612/`。

本轮确认：

1. 壳内 sidecar SHA-256、当前 commit identity 和 WebView2 CDP 一致。
2. 首屏在 20 秒内出现真实 composer，health 为 connected；初始业务请求不超过 5 次且无重复路由。
3. 状态栏的 `backend`、`workspace`、`model`、`permission`、`task` 五个分段均为真实按钮，可键盘聚焦，有非空值和 aria-label。
4. workspace 路径展示已脱敏；项目导航可操作；workspace 输入为只读并正确连接原生选择器。
5. diagnostics ledger 真实加载 14 条、上限 512；8 个诊断项齐全，计数一致，未发现路径、token 或 query 泄漏。
6. 一键诊断包真实生成且通过安全检查。
7. permissions center 的尺寸、来源、完整访问确认卡可用；取消操作不修改配置；overview 只调用一次；无 secret/path 泄漏。
8. model 与 backend 展示值一致且有真实来源。

## 结论边界

本轮证明当前桌面 WebView 的首屏、状态栏、workspace 选择、诊断包、权限中心和键盘路径是真实可交互的，满足 P4 的一部分真机 UI 证据要求；不证明 P4 已整体完成。

仍未纳入本轮完成的事项：

- CLI/TUI 与 Desktop 的完整功能矩阵及等价错误下一步；
- 所有可见高级面板的逐页截图验收；本轮明确跳过截图；
- WorkSwarm/Fleet/Eval/Cloud 等能力的 Stable/Optional/Experimental/Hidden 全量清单收口；
- P3 工具审批、diff/revert 统一闭环；
- P6 release 安装包、隔离安装目录双击黄金路径、manifest 与最终发布归档。

日志中仍出现 `HotKey already registered`，但窗口、WebView、业务请求和交互断言全部通过；当前将其记录为非阻断的全局快捷键冲突提示，后续单独处理。

## 截图增量验收

随后移除 `-SkipScreenshots` 重跑同一脚本，结果：**52/52 通过，exit 0**。

证据目录：
`agent-sdk/docs/qa/evidence/p4-r4-ui-20260922-0713/`。

新增并通过的外观证据：

- 权限中心截图：`r4-ui-permissions.png`，1295×837，像素有效性检查通过；
- 900×600 截图：`r4-ui-900x600.png`，解码、尺寸、字节、像素比例和方差检查通过；
- 1280×720 截图：`r4-ui-1280x720.png`，同上；
- 1920×1080 截图：`r4-ui-1920x1080.png`，实际 WebView 1707×1067，检查通过；
- 三种尺寸下状态条均无横向溢出，五段仍完整存在。

因此，P4 的桌面 WebView 真机交互、键盘路径和截图证据现已具备；CLI/TUI 功能矩阵、默认导航全量成熟度整理及 P6 release 安装包仍不能据此标记完成。
