# -*- coding: utf-8 -*-
"""Final freeze round: resolve the 200-person conflict, unify deployment/team pricing,
harden the input-focus argument, and close terminology gaps."""
from docx import Document

SRC = r"T:\创新创业\OwO-master\docs\Cuttle——情境原生智能输入系统-参赛商业计划书-国金重构版-v8.docx"
doc = Document(SRC)

def set_text(p, text):
    runs = p.runs
    if not runs:
        p.add_run(text); return
    runs[0].text = text
    for r in runs[1:]:
        r.text = ""; r._element.getparent().remove(r._element)

def set_cell(cell, text):
    ps = cell.paragraphs
    set_text(ps[0], text)
    for extra in ps[1:]:
        extra._element.getparent().remove(extra._element)

def set_row(table, ri, values):
    for ci, v in enumerate(values):
        set_cell(table.rows[ri].cells[ci], v)

def find_p(prefix):
    for p in doc.paragraphs:
        if p.text.strip().startswith(prefix):
            return p
    raise KeyError(prefix)

def E(prefix, text):
    p = find_p(prefix); set_text(p, text); return p

# ================= 3 & 4. 技术论证收严 =================
E("第二，输入焦点天然带有 Origin",
  "第二，输入焦点天然提供构建 Origin 的稳定锚点。输入会话同时给出当前应用、窗口、输入控件与光标编辑位置，"
  "结合应用适配器即可确定具体对象与返回位置，这使成果返回与责任追溯在结构上成为可能，而不是事后猜测。"
  "没有这层锚点，Return to Origin 只能退化为让用户自己找地方粘贴。")

E("上述四条说明了输入时刻在信号质量上的优势",
  "上述四条说明了输入时刻在信号质量上的优势，但还需要回答一个更关键的问题——"
  "如果只需要一个全局快捷键加悬浮窗口，是否同样可以完成这些任务。项目认为两者存在实质差别，"
  "并把它作为核心命题中必须被验证的部分。差别不在权限，而在信号的来源与稳定性："
  "输入系统本身处于输入会话之中，可以直接获得显式意图、输入控件与光标编辑位置，"
  "并把它们绑定成同一个任务生命周期的锚点，因此返回位置在任务开始时就已经确定。"
  "全局快捷入口在获得系统级权限后同样可以推断当前焦点，但它需要通过额外的焦点解析、"
  "活动窗口判定与对象绑定来重建这一锚点，且自身并不处于输入会话中，"
  "因此更容易出现对象识别偏差或绑定丢失。项目要验证的正是这项差别在真实任务中是否产生可测的效率与准确性差异。")

E("因此项目把这条命题做成可证伪的实验",
  "因此项目把这条命题做成可证伪的实验，而不是设计主张：对照实验设置三组入口，独立 AI 工作台、全局快捷键 Agent、"
  "输入焦点原生的 Cuttle，在相同模型、工具与任务材料下比较上下文重复说明次数、来源绑定准确率、发起步骤数与结果返回成本。"
  "若三组之间没有可测差异，项目的入口选择即不成立，届时应转向通用 Agent 工作台路线。")

# ================= 5. 场景主副线收口 =================
set_row(doc.tables[6], 2, ["资料核验（主链子任务）", "在文献或资料页面输入“比较这些方法”",
                           "读取选中资料，建立证据矩阵，并请求必要确认",
                           "论点、证据、来源与局限表"])
set_row(doc.tables[6], 3, ["代码修复（副链）", "在 IDE 中输入“修复这个报错并测试”",
                           "读取错误与相关文件，生成计划并执行测试",
                           "可审阅差异、测试结果与回滚点"])

# ================= 6. 表 4 判定指标完整化 =================
set_row(doc.tables[5], 1, ["入口三组对照：输入焦点原生优于独立工作台与全局快捷键",
                           "三组入口同任务对照（工作台 / 全局快捷 Agent / Cuttle），顺序平衡",
                           "主指标：上下文重复说明次数降低 15%；约束：Origin 绑定准确率不下降，发起步骤与返回成本至少一项显著改善",
                           "主指标：上下文重复说明次数降低 30%；约束同上",
                           "缩小入口范围，保留快捷指令"])

# ================= 7. 术语收口 =================
E("4.2 核心创新二 从输入意图到受控行动的受控自治升级闭环",
  "4.2 核心创新二 从输入意图到行动的受控自治升级闭环")
E("表中门槛分为三组",
  "表中门槛分为三组，避免把设计原则与实验统计混为一谈。设计红线属于实现约束，不满足即不允许发布："
  "明确标识的密码框、支付确认与金融字段的访问率须为 0，高风险动作未经授权执行次数须为 0。"
  "测试指标属于红队与故障注入的统计结果，用于度量系统在未知、伪装与边界场景下的泛化能力，"
  "例如伪装敏感字段漏检率不高于 1%、可逆任务恢复成功率不低于 90%、审批影响范围判断正确率不低于 95%。"
  "生产事故指标属于运营阶段的目标值，允许出现个例，但必须分级定责、限时收敛，并在下个版本以回归测试固化。"
  "把三组分开表述的原因很简单：把“绝对为 0”当作泛化能力的统计描述，既不可验证，也会掩盖真实风险。")
set_row(doc.tables[13], 0, ["类别", "代表产品", "强项", "当前主要交互形态与价值重心", "Cuttle 应对"])

# ================= 2. 定价口径统一 =================
set_row(doc.tables[14], 3, ["团队版", "需要统一策略与审计的团队与机构",
                            "成员管理、统一策略、审计、共享技能与技术支持",
                            "4 万元每团队每年起", "第二收入方式；须取得至少 3 份试点意向与首次报价反馈"])
set_row(doc.tables[15], 3, ["私有部署", "18 万元每项目", "8 万元实施与维护", "55%",
                            "交付周期、定制边界与验收条件"])
set_row(doc.tables[20], 2, ["团队许可（个）", "0 / 0 / 0", "3 / 5 / 10", "8 / 24 / 40",
                            "4 万元每团队每年；按路线图自第二年起产生收入"])

# ================= 1. 第一年规模口径 =================
E("本章用于检验商业模型能否形成可持续经营",
  "本章用于检验商业模型能否形成可持续经营，不代表项目已经取得收入、融资或用户规模。"
  "测算采用统一口径，三种情景分别设定用户与客户数量，再按同一单价假设计算收入："
  "个人专业版按年末在付用户数乘年费 240 元确认收入；团队许可按 4 万元每团队每年计价，自第二年起计入；"
  "私有部署按 18 万元每项目、以验收确认收入；每年在付用户由上年在付规模的 60% 续订加当年新增构成。"
  "第一年的规模由第六章的验证容量约束：第一年个人付费用户不超过 200 人，三种情景分别按 80、120、180 人测算，"
  "均在该容量之内；深度服务与完整观察同样按约 200 人配置，因此第一年的重心是验证而非规模。"
  "全部单价与转化率均属待验证假设，验证方式与门槛见表 14。")

doc.save(SRC)
print("freeze round applied")