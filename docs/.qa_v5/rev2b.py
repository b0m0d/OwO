# -*- coding: utf-8 -*-
"""v8 revision round 2, part B: theory and narrative tightening across ch.1-9."""
from docx import Document
from docx.oxml.ns import qn

SRC = r"T:\创新创业\OwO-master\docs\Cuttle——情境原生智能输入系统-参赛商业计划书-国金重构版-v8.docx"
doc = Document(SRC)

def set_text(p, text):
    runs = p.runs
    if not runs:
        p.add_run(text); return
    runs[0].text = text
    for r in runs[1:]:
        r.text = ""; r._element.getparent().remove(r._element)

def set_cell(cell, text):
    ps = cell.paragraphs
    set_text(ps[0], text)
    for extra in ps[1:]:
        extra._element.getparent().remove(extra._element)

def set_row(table, ri, values):
    for ci, v in enumerate(values):
        set_cell(table.rows[ri].cells[ci], v)

def find_p(prefix):
    for p in doc.paragraphs:
        if p.text.strip().startswith(prefix):
            return p
    raise KeyError(prefix)

def E(prefix, text):
    p = find_p(prefix); set_text(p, text); return p

# ---------- 第一章：信任改为“适当信任”，参考文献 [25] ----------
E("这一思路把产品竞争从模型问答能力",
  "这一思路把产品竞争从模型问答能力转向意图承接与任务交付能力。项目不与基础模型厂商争夺模型能力本身，"
  "而是在模型与桌面应用之间构建一层可控、可验证、可替换的意图基础设施。模型能力越强，Cuttle 可调度的执行能力越丰富，"
  "而用户始终保有对情境范围、权限边界与最终结果的控制。判断项目是否成立的最终标准，"
  "不是用户对系统的信任是否不断提高，而是是否形成与系统实际能力相匹配的适当信任："
  "低风险任务敢于授权，高风险任务能够理解、审阅并及时接管。[25][26]")

# ---------- 第二章：表 4 升级为三组入口与新的信任假设 ----------
set_row(doc.tables[5], 1, ["入口三组对照：输入焦点原生优于独立工作台与全局快捷键",
                           "三组入口同任务对照（工作台 / 全局快捷 Agent / Cuttle），顺序平衡",
                           "上下文重复说明次数降低 15%", "上下文重复说明次数降低 30%",
                           "缩小入口范围，保留快捷指令"])
set_row(doc.tables[5], 2, ["意图胶囊能减少重复说明并减少多余读取",
                           "对照任务计时与记录；情境标注集",
                           "Context Recall 不低于 85%，重复输入减少 15%",
                           "Context Recall 不低于 90%，重复输入减少 30%",
                           "减少自动感知，增强手动选区"])
set_row(doc.tables[5], 3, ["受控自治升级优于固定工作台",
                           "同任务计时与接管计数",
                           "中位完成时间降低 10%，过度升级率低于 15%",
                           "中位完成时间降低 25%，过度升级率低于 10%",
                           "只保留高价值任务升级"])
set_row(doc.tables[5], 4, ["可见审批提高影响范围理解并降低误批准",
                           "敏感任务可用性测试",
                           "影响范围判断正确率不低于 85%，误批准率低于 5%",
                           "影响范围判断正确率不低于 95%，误批准率低于 3%",
                           "增加解释与二次确认"])
E("表 4 预设验证标准与证伪条件", "表 4 预设验证标准与证伪条件（与三组对照实验一致）")

# ---------- 第三章：Intent Router 输出 ----------
E("Cuttle 由一个统一入口和四个核心系统组成",
  "Cuttle 由一个统一入口和四个核心系统组成。统一入口承接键盘、拼音、自然语言、语音、选区和快捷指令；"
  "Context Engine 生成最小必要情境；Intent Router 判定自治程度、协作形态与执行位置，并给出审批要求；"
  "Execution Fabric 负责受控执行与按契约协作；Verification and Return 绑定证据、验收成果并把结果返回原始应用。")

# ---------- 第四章：术语统一、Scope 说明、执行位置特征 ----------
E("意图胶囊（Intent Capsule, IC）是本项目最具定义性",
  "意图胶囊（Intent Capsule, IC）是本项目最具定义性的技术对象：它把一次用户输入从一段自然语言，"
  "变成有作用域、有生命周期、可校验、可失效的中间表示，供路由、执行与验证共用。"
  "需要说明的是，IC 首先是一种数据结构，而不是数学模型或评分函数。")

# 在 IC 字段说明后补 Scope 的执行权限上界说明
import copy
from docx.text.paragraph import Paragraph
if not any(p.text.strip().startswith("关于 Scope 需要额外说明一点") for p in doc.paragraphs):
    scope_p = find_p("字段信度（Confidence）是每个字段的元数据")
    el = copy.deepcopy(scope_p._element)
    scope_p._element.addnext(el)
    np = Paragraph(el, scope_p._parent)
    set_text(np, "关于 Scope 需要额外说明一点：Scope 规定的是执行权限的上界，不代表对具体动作的批准。"
                 "它说明系统最多可以读取什么、最多可以执行什么；某个具体动作是否需要审批，"
                 "仍由策略引擎根据执行计划与目标对象决定。因此“可执行范围”与“动作风险”并不冲突："
                 "前者是静态边界，后者是按动作动态计算的判断结果。")

# 术语定义：任务契约
if not any(p.text.strip().startswith("本节涉及两个容易混淆的名词") for p in doc.paragraphs):
    contract_p = find_p("本节的机制共同回答一个问题")
    el2 = copy.deepcopy(contract_p._element)
    contract_p._element.addnext(el2)
    np2 = Paragraph(el2, contract_p._parent)
    set_text(np2, "本节涉及两个容易混淆的名词，在此统一定义：任务契约（Task Contract）描述任务目标、"
                  "允许的动作、验收条件与成本边界，是路由与执行的输入；成果契约（Artifact Contract）是执行者之间传递产物的接口，"
                  "描述来源引用、输出类型与格式约定、版本号、修改内容、证据、验收条件、未解决问题、恢复点与责任执行者。"
                  "两者的区别在于作用层次：任务契约约束“这件事怎么做”，成果契约约束“产物如何交接”。")

# 执行位置决策特征
E("决策的输入特征包括任务复杂度",
  "决策的输入特征包括任务复杂度、动作可逆性、所需数据范围、工具需求、潜在并行性、结果可合并性与成本上限；"
  "判定执行位置时还需加入数据所在地、隐私敏感度、设备能力、网络条件、可信等级与延迟要求；"
  "这些特征可以从任务契约与情境字段中自动提取，因此决策过程可复现、可标注、可评测。"
  "与按任务类型分类的常见路由相比，三维决策同时给出上下文暴露范围、可用工具集、审批方式与成果回填形式四项授权结果。")

E("系统为每一次输入回答三个相互独立的问题",
  "系统为每一次输入回答三个相互独立的问题，而不是把它归入一个层级数字：这次任务需要多大的自治程度；"
  "需不需要拆给多个执行者协作；在哪个设备或环境执行。自治程度取文本表达、工具调用、智能体执行三档；"
  "协作形态取单执行者与多执行者两档；执行位置取本地、云端、跨设备三档。三者在权限、能力与数据位置的约束下组合，"
  "而不是任意自由组合：数据不能离开本地的任务不会路由到云端，权限不足的执行者不会被分配高风险动作。"
  "这一闭环还包括最后一步：成果以可审阅的形式返回需求产生的原位置，即 Return to Origin。")

# 三组对照实验的因果表述
E("三组对照实验的公平性前提必须写清",
  "三组对照实验的公平性前提必须写清：三组使用同一模型与同一版本、相同的工具与权限范围、相同的任务材料与结果要求，"
  "唯一变量是“任务如何进入系统、情境如何获得”这一整体机制，因此效率与质量差异可以归因于入口与情境获取的组合设计。"
  "由于入口与情境获取在工程上相互依赖，本项目不把它拆成入口 × 情境的析因设计，而是先比较整体机制，"
  "再通过消融实验分别估计 IC、自治决策、协作机制与安全授权的贡献。"
  "实验采用同组内交叉设计，同一用户在多轮中依次使用三种入口，并对顺序做平衡处理，以消除学习效应与熟练度差异。")

# 表 7 去掉正式跨设备示例，改为边界示例
set_row(doc.tables[8], 5, ["（边界示例）由手机端发起并回填",
                           "文本表达或工具调用", "单执行者；跨设备",
                           "属远期扩展能力的边界情形，本轮仅做最小数据原则验证"])

# ---------- 第六章：竞品绝对化表述与壁垒标题 ----------
E("输入法产品正在增加续写",
  "输入法产品正在增加续写、帮写、润色、翻译与窗口跟随能力。搜狗输入法 2026 年更新已提供 AI 光标助手和多项 AI 帮写服务。[6]"
  "Wispr Flow 的 Context Awareness 会读取光标附近文本与活动应用，按场景调整转写与风格。[7]"
  "另一侧，Codex 与 MiMo Desktop 等工作台型产品已经提供长任务、多 Agent、技能与桌面执行能力。[8][12]"
  "需要说明的是，这些产品的能力边界正在快速移动，本项目不以“竞品没有某项能力”作为差异来源，"
  "而以当前可观察的交互形态作为判断依据：它们当前的主要交互形态是独立 Agent 工作空间或显式任务入口，"
  "主要价值集中于任务执行本身，而不是输入焦点原生的任务生命周期；输入法类产品当前的主要价值仍集中于输入表达效率。"
  "Cuttle 的差异在于把输入焦点、持续情境、分级执行、成果验证与返回原处组成一条系统链路。")

set_row(doc.tables[13], 1, ["AI 输入法", "搜狗、讯飞、百度",
                            "高频入口、补全改写、语音",
                            "当前主要价值集中于输入与表达效率",
                            "把输入连续升级为受控任务"])
set_row(doc.tables[13], 2, ["全局语音输入", "Wispr Flow", "跨应用输入、情境化表达",
                            "当前主要交互形态是跨应用转写与风格适配",
                            "扩展至任务路由与成果返回"])
set_row(doc.tables[13], 3, ["通用 AI 助手", "ChatGPT、Claude", "模型能力与知识服务",
                            "当前主要交互形态是独立对话与工作空间",
                            "用意图胶囊减少重复说明"])
set_row(doc.tables[13], 4, ["Agent 工作台", "Codex、MiMo Desktop、Claude Code",
                            "长任务、多 Agent、桌面执行",
                            "当前主要交互入口是显式任务与项目工作区",
                            "作为桌面任务型 Agent 的可选前置意图入口"])
set_row(doc.tables[13], 5, ["自动化平台", "RPA 与工作流工具", "确定性流程与企业集成",
                            "当前主要目标是流程确定性与企业集成",
                            "自然语言路由与可验证执行"])
h63 = find_p("6.3 竞争壁垒")
set_text(h63, "6.3 可积累优势与拟形成壁垒")
h63.style = doc.styles["Heading 2"]
E("三道壁垒都在建设过程中",
  "上述三道优势目前均在建设过程中，不作为既成壁垒主张；它们真正的成本来自把协议、可靠性与真实数据同时做出来所需的时间，"
  "因此本节标题为“可积累优势与拟形成壁垒”，而非已经形成的壁垒。")

doc.save(SRC)
print("part B done; paras", len(doc.paragraphs))