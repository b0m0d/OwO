# -*- coding: utf-8 -*-
"""v8 stage 2 : chapters 2-6."""
import copy
from docx import Document
from docx.text.paragraph import Paragraph

SRC = r"T:\创新创业\OwO-master\docs\Cuttle——情境原生智能输入系统-参赛商业计划书-国金重构版-v8.docx"
doc = Document(SRC)
edits = 0

def set_text(p, text):
    runs = p.runs
    if not runs:
        p.add_run(text); return p
    runs[0].text = text
    for r in runs[1:]:
        r.text = ""
    return p

def find_p(prefix, style=None):
    for p in doc.paragraphs:
        if p.text.strip().startswith(prefix) and (style is None or p.style.name == style):
            return p
    raise KeyError(prefix)

def set_cell(cell, text):
    ps = cell.paragraphs
    set_text(ps[0], text)
    for extra in ps[1:]:
        extra._element.getparent().remove(extra._element)

def set_row(table, ri, values):
    for ci, v in enumerate(values):
        set_cell(table.rows[ri].cells[ci], v)

def E(prefix, text, style=None):
    global edits
    p = find_p(prefix)
    if style:
        p.style = doc.styles[style]
    set_text(p, text)
    edits += 1
    return p

def clone_after(ref_p, text, style='Normal'):
    el = copy.deepcopy(ref_p._element)
    ref_p._element.addnext(el)
    np = Paragraph(el, ref_p._parent)
    for r in list(np.runs)[1:]:
        r._element.getparent().remove(r._element)
    np.style = doc.styles[style]
    set_text(np, text)
    return np

# ---------------- 第二章 ----------------
E("项目采用窄场景切入",
  "本项目采用窄场景切入。产品服务的高频任务，是在多个信息来源之间取数、核对口径并形成可交付材料；"
  "承担这类任务的岗位包括运营、项目、咨询、研究、行政与产品，它们的共同特征是需要跨应用获取信息、"
  "对关键结论负责并反复交付文档。首批只保留两条基准任务链：主链为“跨来源资料取数与口径核验—报告交付”，"
  "副链为“代码问题—修改—测试—可审阅差异”。两条链路都满足输入焦点明确、任务边界可描述、结果可验收三个条件，"
  "适合作为配对实验与纵向分析的起始样本。")

E("这里需要区分两件事",
  "学生与科研人员是上述用户群的重要子集，也是团队最先能够触达的样本来源，但不作为产品定位的依据。"
  "本项目不以单点文本生成任务作为主场景，因为这类需求由通用写作工具即可覆盖；"
  "跨来源取数与口径核验才需要持续的情境、来源追溯与受控执行，也才是这套机制真正被需要的地方。")

E("两组数据回答不同问题，不可混用",
  "两个样本组回答的并不是同一个问题，因此结果分别报告：纵向组追踪同一批用户在熟练之后的稳定价值、"
  "信任变化与流失原因，用于判断机制能否长期成立；独立组此前未参与任何设计与原型讨论，"
  "其首用数据不含学习效应与期待偏差，用于判断新用户能否自行理解并接受这套机制。"
  "统计口径在数据收集前确定：各指标的最低可接受值与目标值来自预实验，"
  "再依据预实验的效应量做统计功效分析，据此确定正式实验所需样本量；"
  "组间比较报告效应量与置信区间，而不只报告均值差异。")

# 表 3
t = doc.tables[4]
set_row(t, 0, ["用户与岗位", "高频任务", "现有障碍", "首个验证场景"])
set_row(t, 1, ["运营、项目、咨询、研究、行政、产品（主场景）",
               "跨来源取数、口径核验、报告与材料交付",
               "反复复制背景；来源与证据难以回溯",
               "跨来源资料到分析与 Word 报告（主链）"])
set_row(t, 2, ["数据与研发人员（副场景）",
               "代码修复、测试与文档",
               "工具切换多；失败恢复难",
               "错误到修复、测试与可审阅差异（副链）"])
set_row(t, 3, ["高校学生与科研人员（早期样本）",
               "资料检索、综述与项目材料",
               "重复格式工作；审核链条长",
               "作为独立验证组的样本来源，不单独设定场景"])
set_row(t, 4, ["实验室与机构（后续扩展）",
               "数据整理、材料归档、多人协作",
               "模型与工具分散；缺少权限治理",
               "本轮不纳入，仅作为团队许可的需求来源"])

# ---------------- 第四章：创新收敛 ----------------
E("意图胶囊（Intent Capsule, IC）是本项目唯一的核心技术对象",
  "意图胶囊（Intent Capsule, IC）是本项目最具定义性的技术对象：它把一次用户输入从一段自然语言，"
  "变成有作用域、有生命周期、可校验、可失效的中间表示，供路由、执行与验证共用。"
  "需要说明的是，IC 首先是一种数据结构，而不是数学模型或评分函数。")

E("信度 Confidence 与动作风险 Risk 不放进意图胶囊",
  "因此 IC 只包含六个字段：Intent、Context、Origin、Provenance、Freshness、Scope。"
  "字段信度是指述性的元数据，用于标注某个字段是否足以支撑自动执行，它本身不触发任何动作。"
  "动作风险取决于系统准备做什么，只能在生成执行计划时依据具体动作与目标对象判定，"
  "因此完全归策略引擎处理，不进入 IC。同一条意图在“只读取文件”与“覆盖原文件”两种计划下风险完全不同，"
  "把风险写入 IC 会掩盖这一区别。全文出现的 IC 均指上述六个字段的集合，不再使用其他写法。")

E("定义之后需要说明它为什么必要",
  "定义之后需要说明它为什么必要。现有方案存在三个结构性缺陷：每次请求都要重新交代背景，"
  "因为工作台不在任务现场；情境读取没有边界，因为系统不知道读取是为了哪一件事；任务结束后上下文残留，"
  "因为没有失效条件。IC 分别用 Origin、Context、Freshness 三个字段对应解决上述缺陷，"
  "并把来源与权限边界写成可校验字段，而不是工程约定。")

E("意图胶囊自身的评价对象是情境质量",
  "IC 自身的评价对象是情境质量，而不是路由准确率：来源绑定是否正确（Origin Binding Accuracy）、"
  "最小情境是否漏读（Context Recall）、是否多读（Context Precision 与情境过度读取率）、"
  "是否仍在使用已失效上下文（Stale Context Misuse Rate）、证据是否完整（Provenance Coverage），"
  "以及用户是否需要重复交代背景（Context Re-entry Reduction）。路由类指标属于 4.2 节的评价对象。")

# 表 7 IC 字段定义
t = doc.tables[7]
set_row(t, 0, ["IC 字段", "含义", "作用", "失效事件", "用户控制"])
set_row(t, 6, ["Scope", "Scope and Consent Boundary",
               "限定可读取范围与可执行范围，并记录用户的同意边界",
               "授权变更、范围外读取请求", "确认、拒绝、撤回授权"])

# 4.2 标题与三维措辞
E("4.2 核心创新二 三维自洽决策 自治程度 协作形态 执行位置",
  "4.2 核心创新二 从输入意图到受控行动的连续自治闭环")
E("系统需要为每一次输入回答三个相互独立的问题",
  "系统为每一次输入回答三个相互独立的问题，而不是把它归入一个层级数字：这次任务需要多大的自治程度；"
  "需不需要拆给多个执行者协作；在哪个设备或环境执行。自治程度取文本表达、工具调用、智能体执行三档；"
  "协作形态取单执行者与多执行者两档；执行位置取本地、云端、跨设备三档，三者可以自由组合。"
  "这一闭合回路还包括最后一步：成果以可审阅的形式返回需求产生的原位置，即 Return to Origin。")

# 表 8 三维决策示例
t = doc.tables[8]
set_row(t, 0, ["任务示例", "自治程度", "协作形态与执行位置", "判定依据与验证指标"])
set_row(t, 1, ["补全一句话或调整语气",
               "文本表达（最低档）",
               "单执行者；本地",
               "复杂度低且动作可逆；指标为接受率、修改量与过度升级率"])
set_row(t, 2, ["按选区生成表格摘要",
               "工具调用",
               "单执行者；本地",
               "工具依赖明确且结果可预览；指标为成功率与时延"])
set_row(t, 3, ["修复报错并补测试",
               "智能体执行",
               "单执行者；本地加沙箱",
               "串行多步骤但并行收益不明显；指标为完成率、接管率与成本"])
set_row(t, 4, ["分别阅读两篇论文并合并摘要",
               "智能体执行",
               "多执行者；云端",
               "可并行且成果可归并；指标为净收益、冲突率与合并失败率"])
set_row(t, 5, ["让手机读取一张照片并回填",
               "文本表达或工具调用",
               "单执行者；跨设备",
               "自治需求低而数据位置约束强；指标为传输量与成功率"])

# 4.3 降级为执行优化策略
E("4.3 核心创新三 自适应组队与版本化成果契约",
  "4.3 支撑机制 版本化成果契约与执行效率优化")
E("Cuttle 不默认组建多 Agent",
  "版本化成果契约是本节的主要机制。多执行者之间不以自由对话传递状态，而以契约接力："
  "成果契约是一个数据结构，字段包括来源引用、输出类型与格式约定、版本号、修改内容、证据、验收条件、"
  "未解决的问题、恢复点与责任执行者；下游只有在前置验收条件满足后才接收成果，"
  "因此上下文漂移与责任模糊会表现为可检测的结构化偏差，而不是难以定位的失败。")
E("执行前的组队评分由任务可并行性",
  "自适应组队属于执行效率优化，不作为独立创新点主张。多执行者协作在多数任务上协调开销大于收益，"
  "因此系统默认单执行者，仅在预测收益为正时才考虑组队。执行前的组队判断由任务可并行性、"
  "结果可合并性、预计耗时收益、协调开销、合并难度与风险暴露共同决定，"
  "前两项高而后两项低时才适合组队；该判断使用任务特征与历史实测结果，而不是固定的产品层级。")
E("执行后的实测收益是真实可观测的量",
  "执行后的实测收益是真实可观测的量：相对单执行者的质量变化、墙钟耗时变化、额外推理成本、"
  "结果冲突与合并失败次数。它不参与运行前决策，只用于判定本次组队是否值得，"
  "并作为标签校准执行前的判断，由此形成预测、决策、执行与反馈校准的封闭回路。"
  "若实测收益与事前判断长期不相关，项目将取消预测式组队，改为按任务模板静态指派。")
E("多执行者之间不以自由对话传递状态",
  "本节的机制共同回答一个问题：当任务确实需要拆解时，如何让协作结果可核对、可回溯、可合并。"
  "它们提升既有执行路径的可靠性，而不是提出新的入口范式，因此与 4.1、4.2 两个核心创新在表述上分开。")

# 4.4
E("返回原处与策略、验证、恢复体系不作为创新点主张",
  "返回原处与策略、验证、恢复体系不单独作为创新点主张，而是上述两项机制能否落地的前提。"
  "返回原处机制按原始应用、任务契约与动作风险，把成果转换为可审阅段落、批注、文档副本、回复候选、"
  "差异与回滚点等形态，并保留从原始对象到最终结果的可追溯链路；"
  "策略、验证与恢复体系负责审批独立、动作可回滚、失败可解释，并把每次执行转化为下一次决策的证据。")

# 表 9 三组对照实验
E("表 8 核心机制的对照与消融设计", "表 8 三组对照实验与消融实验设计")
t = doc.tables[9]
set_row(t, 0, ["实验", "对照组", "处理组", "主指标"])
set_row(t, 1, ["入口对照",
               "独立 AI 工作台；全局快捷键 Agent",
               "输入焦点原生的 Cuttle",
               "上下文重复说明次数、来源绑定准确率、发起步骤数、结果返回成本"])
set_row(t, 2, ["意图胶囊消融",
               "无情境传递；仅传递整屏内容",
               "完整 IC（六字段）",
               "Context Recall 与 Precision、来源绑定正确率、失效上下文误用率、情境过度读取率"])
set_row(t, 3, ["自治决策消融",
               "所有任务固定为单 Agent 或固定档位",
               "三维判定加策略引擎审批",
               "过度升级率、高风险低估率、单任务成本、端到端时延"])
set_row(t, 4, ["协作机制消融",
               "固定多执行者且自由对话接力",
               "版本化成果契约加阈值式组队",
               "净收益、冲突率、合并失败率、返工次数"])
set_row(t, 5, ["安全授权消融",
               "由 Agent 自主授权",
               "独立审批加最小权限",
               "越权拦截率、误审批率、禁止访问字段访问率"])

E("对照实验的公平性前提必须写清",
  "三组对照实验的公平性前提必须写清：三组使用同一模型与同一版本、相同的工具与权限范围、"
  "相同的任务材料与结果要求，唯一变量是“任务如何进入系统、情境如何获得”，因此效率与质量差异可以归因于入口设计。"
  "若任一对照组的工具权限被削减或模型版本更低，该结论即不成立。"
  "实验采用同组内交叉设计，同一用户在多轮中依次使用三种入口，并对顺序做平衡处理，以消除学习效应与熟练度差异。")

# ---------------- 第五章 ----------------
E("除通用安全目标外，项目把两个与输入法入口强相关",
  "除通用安全目标外，项目把两个与输入法入口强相关的风险作为专有指标管理，"
  "并把“禁止访问”与“读取过多”拆成两个不同指标：禁止访问字段访问率针对明确标识的密码框、"
  "支付确认与金融敏感字段，属设计红线，只能是 0；情境过度读取率针对普通工作情境中读取了非必需数据，"
  "属统计指标，目标低于 5%。两者的判定依据、标注规则与报告条件不同，不能合并为一个数字。")

# ---------------- 第六章 ----------------
E("市场规模采用自下而上的三层测算",
  "本章不采用“大盘乘比例”的市场推算法。大盘数据只用于说明 AI 使用已进入大规模扩散阶段，"
  "不作为可服务市场的计算基数。团队需要的第一个数字不是市场有多大，而是第一年能够真实服务多少用户，"
  "因此表 11 给出的是第一年试点容量测算：从团队可直接影响的最小样本单元出发，"
  "逐层经过情境筛选、原型试用与付费意愿转化，得到第一年可支撑完整验证的用户规模，"
  "并逐项记录基线耗时与留存数据。可服务市场与潜在市场在该容量被验证之前不下结论。")

E("表 11 第一年试点容量测算", "表 11 第一年试点容量与验证容量测算")
t = doc.tables[12]
set_row(t, 0, ["测算层级", "测算对象", "测算过程与依据", "结果与使用方式"])
set_row(t, 1, ["第一年可交付容量（测算主体）",
               "团队能直接服务并完整观察的用户数",
               "目标学院相关人群约 3000 人，其中高频 PC 资料与报告任务者按调研比例约 40% 得约 600 人，"
               "愿意试用原型约 50% 得约 300 人，可稳定纳入试点观察约三分之二",
               "约 200 人。按此容量配置试点人力，并逐人记录基线耗时与任务日志"])
set_row(t, 2, ["深度与对照容量",
               "可支撑实验设计的样本规模",
               "纵向核心组取 15 至 25 人全程跟踪；独立验证组另招 20 至 30 人；其余进入试用与需求访谈",
               "50 至 80 人可形成深度数据，其中 35 至 55 人进入正式实验"])
set_row(t, 3, ["后续外推路径（待验证）",
               "首校试点复制率验证之后的可服务规模",
               "外推的前提是复核率不低于预设阈值、交付人力可复制、支持成本可核算；"
               "三项条件下每校可服务 200 至 400 人",
               "首校复制率验证完成后重新测算，本轮不下结论"])

para = find_p("表 11 第一年试点容量与验证容量测算")
note = clone_after(para,
  "试点容量的用途是配置研发与人力，而不是对外承诺收入规模。表中的比例均来自前期调研与访谈，"
  "在正式试点开始后由真实转化数据替换；若试点转化率低于预设下限，项目将缩小场景范围，"
  "而不是调整分母使数字成立。")

doc.save(SRC)
print("stage2 ok edits=", edits, "paras", len(doc.paragraphs), "tables", len(doc.tables))