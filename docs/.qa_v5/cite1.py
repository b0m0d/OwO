# -*- coding: utf-8 -*-
"""Citation cleanup: cite sources at genuine design points; rebuild the reference list to 22 cited entries."""
import copy, re
from docx import Document
from docx.text.paragraph import Paragraph

SRC = r"T:\创新创业\OwO-master\docs\Cuttle——情境原生智能输入系统-参赛商业计划书-国金重构版-v8.docx"
doc = Document(SRC)

def set_text(p, text):
    runs = p.runs
    if not runs:
        p.add_run(text); return
    runs[0].text = text
    for r in runs[1:]:
        r.text = ""; r._element.getparent().remove(r._element)

def find(prefix):
    for p in doc.paragraphs:
        if p.text.strip().startswith(prefix):
            return p
    raise KeyError(prefix)

def E(prefix, text):
    set_text(find(prefix), text)

# ---------- 1) 在真实设计处补引用 ----------
# IC / 情境计算
E("意图胶囊（Intent Capsule, IC）是本项目最具定义性的技术对象",
  "意图胶囊（Intent Capsule, IC）是本项目最具定义性的技术对象，也是面向任务生命周期的结构化中间表示："
  "它把一次用户输入从一段自然语言，转换为有作用域、有生命周期、可校验、可失效的中间表示，供路由、执行与验证共用。"
  "情境计算领域对情境的定义与使用已有系统研究，本项目的最小情境设计遵循其中关于情境与任务相关的判断标准。[21][22]")

# 三维任务决策 / 混合主动
E("决策的输出是三维取值与策略引擎裁定的审批方式",
  "决策的输出是三维取值与策略引擎裁定的审批方式：无需确认、先预览、必须确认或直接拒绝。"
  "审批强度的判定沿用混合主动交互中关于自动化程度与用户介入时机的基本原则。[23]"
  "维度拆分的必要性可用一个例子说明：让手机读取一张照片，属于低自治、单执行者、跨设备，"
  "按单一层级会被高估为最高档；而多执行者阅读资料并只输出摘要的任务风险很低，"
  "其等级不应高于自动修改版本库的单执行者任务。")

# 协作机制
E("自适应组队用于执行效率优化",
  "自适应组队用于执行效率优化。现有多智能体框架已能完成角色分工与对话式协作，"
  "但在协调开销、结果合并与责任归属上缺少结构化约束。[16][17]"
  "多执行者协作在多数任务上协调开销大于收益，因此系统默认单执行者，仅在预测收益为正时才考虑组队；"
  "判断依据是任务可并行性、结果可合并性与预计协调成本，并使用历史实测结果持续校准。"
  "若实测收益与事前判断长期不相关，项目将改为按任务模板静态指派。")

# 执行计划与工具调用
E("当特征不确定性较高或动作不可逆时",
  "执行计划由规划与工具调用两类能力共同支撑：推理与行动交错的范式用于生成可解释的执行步骤，"
  "工具调用能力用于把步骤落到具体接口上。[13][14]"
  "当特征不确定性较高或动作不可逆时，系统采用保守决策原则：降低自治程度、维持单执行者、"
  "优先在数据所在位置执行，并要求用户确认。该原则用于让过度升级率与高风险低估率同时可观测、可调参。"
  "执行失败时以反思式重试替代盲目重复，并把失败原因写入恢复记录。[15]")

# Windows 实现边界
E("首阶段以 Windows 桌面环境为主要验证平台",
  "首阶段以 Windows 桌面环境为主要验证平台，优先覆盖 Word、主流浏览器与主流 IDE，"
  "依托文本服务框架与 UI Automation 完成输入焦点识别、控件读写与成果回填；[27]"
  "其他桌面平台在核心机制验证完成后逐步适配，不对当前阶段作跨平台承诺。")

# 安全与治理
E("项目按动作影响范围划分四档风险",
  "项目按动作影响范围划分四档风险，并对应不同的控制策略。"
  "风险分级与治理流程参照人工智能风险管理框架中的治理与度量要求，"
  "人机交互部分参照人机交互设计准则与以人为本的人工智能原则。[24][28][30]"
  "低风险包括补全、翻译与语气调整，由本地候选生成，用户可忽略，系统保留修改量记录；"
  "中风险包括读取选区与生成文档副本，系统显示读取范围并要求先预览，原件不被覆盖且操作可撤销；"
  "高风险包括写文件、执行命令与访问网络，必须经过独立审批，工具与路径受限定，并保留差异、日志与回滚点；"
  "发送消息、删除普通文件等动作要求二次确认并保留完整审计信息。"
  "支付、转账、密码输入及其他资金操作不纳入当前版本的可执行范围。\n"
  "在此基础上，项目把两个与输入法入口强相关的风险作为专有指标管理。禁止访问与读取过多属于不同性质的指标："
  "禁止字段访问率针对明确标识的密码框与金融敏感字段，属于设计红线，取值为 0；"
  "情境过度读取率针对普通工作情境中读取了非必需数据，属于统计指标，目标低于 5%，"
  "两者依据不同的判定规则分别报告。")

# 适当信任 -> 移到安全章，改成对信任文献的引用
E("这一思路把产品竞争从模型问答能力",
  "这一思路把产品竞争从模型问答能力转向意图承接与任务交付能力。项目不与基础模型厂商争夺模型能力本身，"
  "而是在模型与桌面应用之间构建一层可控、可验证、可替换的意图基础设施。模型能力越强，"
  "Cuttle 可调度的执行能力越丰富，而用户始终保有对情境范围、权限边界与最终结果的控制。"
  "判断项目是否成立的最终标准，是是否形成与系统实际能力相匹配的适当信任："
  "低风险任务敢于授权，高风险任务能够理解、审阅并及时接管；自动化信任研究表明，"
  "信任的设计目标应与系统可靠性对齐，而非单纯追求提高。[25][26]")

# 8.2 团队能力：删掉与工程能力无关的引用位（无编号变化）

doc.save(SRC)
print("citations inserted")