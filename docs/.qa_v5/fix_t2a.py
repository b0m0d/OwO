# -*- coding: utf-8 -*-
import copy
from docx import Document
from docx.text.paragraph import Paragraph

SRC = r"T:\创新创业\OwO-master\docs\Cuttle——情境原生智能输入系统-参赛商业计划书-国金重构版-v8.docx"
doc = Document(SRC)

def set_text(p, text):
    runs = p.runs
    if not runs:
        p.add_run(text); return
    runs[0].text = text
    for r in runs[1:]:
        r.text = ""; r._element.getparent().remove(r._element)

def find(prefix):
    for p in doc.paragraphs:
        if p.text.strip().startswith(prefix):
            return p
    raise KeyError(prefix)

def E(prefix, text):
    set_text(find(prefix), text)

def drop(prefix):
    p = find(prefix); p._element.getparent().remove(p._element)
    print("  dropped:", prefix[:36])

def clone_after(anchor, text):
    model = None
    for p in doc.paragraphs:
        if p.style.name == "Normal" and len(p.text) > 60:
            model = p; break
    el = copy.deepcopy(model._element)
    anchor._element.addnext(el)
    np = Paragraph(el, anchor._parent)
    set_text(np, text)
    return np

E("面向 AI Agent 时代的新一代智能输入法", "从自然语言输入到可验证智能体行动")

drop("项目围绕一条主线展开：真实问题是")
E("项目形成两项核心创新",
  "项目形成两项核心创新。其一是意图胶囊（IC），一种面向输入时刻的结构化中间表示，"
  "用于统一承载意图、最小情境、来源位置、证据来源、时效信息与授权边界，具备可校验、可失效、可追溯三个特性。"
  "其二是受控自治升级闭环：系统依据任务特征判定自治程度、协作形态与执行位置，把一次输入从文本表达升级为受控行动，"
  "并以返回原处（Return to Origin）把成果送回需求产生的应用与位置。"
  "版本化成果契约、返回原处机制与策略、验证、恢复体系构成支撑两项机制落地的工程体系。")

E("第一，输入是显式意图",
  "第一，用户主动提交给 Cuttle 的输入是高质量的显式意图信号。屏幕内容只说明用户看到了什么，"
  "而主动发起的输入说明用户此刻想表达什么；前者是被动观察，后者是主动声明，"
  "两种信号的噪声结构与获取成本完全不同。系统接收的不再是猜测出来的可能相关，而是用户刚刚说出口的目标。")

anchor = find("Cuttle 由一个统一入口和四个核心系统组成")
n1 = clone_after(anchor,
  "普通输入链路与智能体链路在架构上分离。常规键盘与拼音输入保持本地快速路径，"
  "不触发情境读取，也不调用模型，输入延迟与既有输入法一致；"
  "仅当用户通过专用快捷键、候选栏入口、选区指令或显式任务命令主动发起时，"
  "系统才建立意图胶囊并进入智能体执行流程。"
  "这一设计同时解决三个问题：避免误触发、控制隐私暴露面、保证打字链路的响应速度。")
n2 = clone_after(n1,
  "首阶段以 Windows 桌面环境为主要验证平台，优先覆盖 Word、主流浏览器与主流 IDE，"
  "依托文本服务框架与 UI Automation 完成输入焦点识别、控件读写与成果回填；"
  "其他桌面平台在核心机制验证完成后逐步适配，不对当前阶段作跨平台承诺。")

E("3.2 典型工作场景", "3.2 核心验证场景")
drop("资料核验。用户在文献或资料页面选中条目后输入")
drop("沟通表达。用户在聊天框中输入")
p_last = find("代码修复。用户在 IDE 中输入")
clone_after(p_last,
  "低风险的文本表达、资料比较与格式整理沿用同一入口，但不作为核心验证场景；"
  "它们用于验证链路稳定性与用户留存，不用于证明项目的差异点。")
E("项目汇报。用户在 Word 中写好材料标题后输入",
  "案例 A：跨来源资料到 Word 报告。用户在 Word 中写好材料标题后输入“按这些资料更新汇报口径”，"
  "系统识别文档结构与格式要求，调用检索、核验与写作流程，在目标文档中生成带来源标注与修改痕迹的内容，"
  "关键数字保留可追溯的证据链。")
E("代码修复。用户在 IDE 中输入“修复这个报错并测试”",
  "案例 B：IDE 错误到可验证差异。用户在 IDE 中输入“修复这个报错并测试”，"
  "系统读取错误信息与相关文件，生成计划并执行测试，返回可审阅差异、测试结果与回滚点。")

E("字段信度（Confidence）是每个字段的元数据",
  "字段信度（Confidence）是每个字段的元数据，用于标注该字段是否足以支撑自动执行，它本身不触发任何动作。"
  "动作风险取决于系统准备做什么，只能在生成执行计划时依据具体动作与目标对象判定，因此完全归策略引擎处理，不进入 IC。"
  "同一条意图在只读取文件与覆盖原文件两种计划下风险完全不同。")
E("关于 Scope 需要额外说明一点",
  "Scope 定义系统可读取与可执行范围的权限上界，具体动作是否需要审批，"
  "则由策略引擎根据执行计划和目标对象动态判定。")

E("返回原处与策略、验证、恢复体系不单独作为创新点主张",
  "返回原处、策略控制、结果验证与失败恢复共同构成两项核心创新的工程支撑体系。"
  "返回原处机制按原始应用、任务契约与动作风险，把成果转换为可审阅段落、批注、文档副本、回复候选、差异与回滚点等形态，"
  "并保留从原始对象到最终结果的可追溯链路；策略、验证与恢复体系负责审批独立、动作可回滚、失败可解释，"
  "并把每次执行转化为下一次决策的证据。")
E("上述实现构成团队承担本项目核心研发的工程基础",
  "上述实现构成团队承担本项目核心研发的工程基础。项目核心创新集中于意图胶囊与受控自治升级闭环，"
  "而运行时、权限、审计、回滚与多 Worker 等基础层是这两项机制能够被真实执行的前提。"
  "后续研发重点集中在输入入口、意图胶囊、返回原处、自治升级阈值与真实用户验证五个方向。")

E("三组对照实验的公平性前提必须写清",
  "为保证三组对照的可比性，各组使用相同的模型版本、工具权限、任务材料与结果要求，"
  "仅改变任务入口与情境获取机制，因此效率与质量差异可归因于入口与情境获取的组合设计。"
  "由于入口与情境获取在工程上相互依赖，项目不采用入口与情境的析因设计，"
  "而是先比较整体机制，再通过消融实验分别估计 IC、自治决策、协作机制与安全授权的贡献。"
  "实验采用同组内交叉设计，同一用户在多轮中依次使用三种入口并对顺序做平衡处理，以消除学习效应与熟练度差异。")

E("表中门槛分为三组",
  "安全指标分为设计红线、测试指标与生产事故指标三类。设计红线属于发布约束，"
  "高风险动作未经授权执行次数与明确敏感字段访问次数必须为 0。测试指标用于评估未知与边界场景下的泛化能力，"
  "包括伪装敏感字段漏检率不高于 1%、可逆任务恢复成功率不低于 90%、审批影响范围判断正确率不低于 95%。"
  "生产事故指标用于衡量上线后的实际运行风险，纳入分级处置与回归测试。")
E("项目按动作影响范围划分四档风险",
  "项目按动作影响范围划分四档风险，并对应不同的控制策略。低风险包括补全、翻译与语气调整，由本地候选生成，"
  "用户可忽略，系统保留修改量记录；中风险包括读取选区与生成文档副本，系统显示读取范围并要求先预览，"
  "原件不被覆盖且操作可撤销；高风险包括写文件、执行命令与访问网络，必须经过独立审批，工具与路径受限定，"
  "并保留差异、日志与回滚点；发送消息、删除普通文件等动作要求二次确认并保留完整审计信息。"
  "支付、转账、密码输入及其他资金操作不纳入当前版本的可执行范围。\n"
  "在此基础上，项目把两个与输入法入口强相关的风险作为专有指标管理。禁止访问与读取过多属于不同性质的指标："
  "禁止字段访问率针对明确标识的密码框与金融敏感字段，属于设计红线，取值为 0；"
  "情境过度读取率针对普通工作情境中读取了非必需数据，属于统计指标，目标低于 5%，"
  "两者依据不同的判定规则分别报告。")

doc.save(SRC)
print("batch A done")